# @package data.data_normalizer

# Configuration for normalizing the processed data.
# Choose which type of normalization to use and give hyperparameters of that
# normalization scheme, if any.
# For the different available options, look through ~/src/data/components/normalization.py

_target_: src.data.components.normalization.L1DataNormalizer
name: robust
hyperparams:
  percentiles: [0.05, 0.95]
