# @package evaluation.callbacks.main

# Reference only: not selected by any defaults list.
# References src.evaluation.callbacks.pu.BkgRatePileup, which is not implemented.
# The metric names need be specified like they are given by the model.
_target_: src.evaluation.callbacks.pu.BkgRatePileup
output_name: "loss/total/full"
target_rates: [0.01, 0.1, 0.25, 0.5, 1]
base_rate: 28608.8064
datamodule: true
log_raw_mlflow: true
