# @package _global_

defaults:
  - override /data: axov4
  - override /algorithm: axov4
  - override /callbacks: basic
  - override /trainer: gpu0
  - override /logger: mlflow

tags: ["vae", "axo_v4", "scan"]

seed: 123

train: true
test: false

trainer:
  min_epochs: 1
  max_epochs: 480

logger:
  mlflow:
    experiment_name: axov4_reproduction
    run_name: test
