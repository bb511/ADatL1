# @package _global_

defaults:
  - /experiment/cchamber/base_agnostic
  - override /algorithm: realnvp

tags: ["causal_chamber", "realnvp", "agnostic"]

algorithm:
  target_rate: 0.01
  base_rate: null
  flow:
    input_dim: 11
    n_flows: 6
    hidden_dim: 32
    n_hidden_layers: 2
