# @package _global_

defaults:
  - /experiment/cchamber/base_agnostic
  - override /algorithm: vae

tags: ["causal_chamber", "vae", "agnostic"]

algorithm:
  target_rate: 0.01
  base_rate: null
  kl_warmup_frac: 0.1
  kl_scale: 0.1
  encoder:
    in_dim: 11
    nodes: [32, 16, 4]
