# @package _global_

defaults:
  - /experiment/cchamber/search_base
  - override /algorithm: vae

tags: ["causal_chamber", "vae", "label_free_search"]

algorithm:
  target_rate: 0.01
  base_rate: null
  kl_scale: 0.001
  kl_warmup_frac: 0.1
  encoder:
    in_dim: 11
    nodes: [32, 16, 4]
