# @package _global_

defaults:
  - override /paths: axov4
  - override /data: classifier_5sig
  - override /model: classifier
  - override /callbacks: classifier
  - override /trainer: gpu0
  - override /logger: mlflow

tags: ["binary_classifier", "axo_v4_encoder", "scan"]

seed: 123

trainer:
  min_epochs: 1
  max_epochs: 480

logger:
  mlflow:
    experiment_name: classifier_5signals
    # run_name: test
    run_name: scan_lr${model.optimizer.lr}
