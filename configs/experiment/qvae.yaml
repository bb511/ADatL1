# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /paths: L1AD
  - override /data: L1AD
  - override /model: qvae
  - override /callbacks: default
  - override /trainer: cpu
  - override /logger: both

tags: ["qvae"]

seed: 12345

trainer:
  min_epochs: 1
  max_epochs: 50

data:
  num_workers: 0

logger:
  mlflow:
    experiment_name: test
    run_name: test

  # wandb:
  #   entity: viictorjimenezzz-personal
  #   project: adatl1

  #   group: debug
  #   name: coderuns
