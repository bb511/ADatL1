# @package _global_

defaults:
  - override /data: axov4
  - override /algorithm: vicreg
  - override /callbacks: default
  - override /trainer: gpu0
  - override /logger: mlflow
  - override /evaluator: default
  - override /evaluator_callbacks: default

seed: 123
tags: ["axo_v6", "vae"]

experiment_name: vicreg
run_name: test

metrics:
  anomaly_rate: null
  anomaly_efficiency: null

callbacks:
  anomaly_rate: null
  last_epoch: null

evaluator:

  ckpts:
    single:
      loss: ['min']


evaluator_callbacks:
  anomaly_efficiency: null
  test_loss:
    _target_: src.evaluation.callbacks.loss.TestLossCallback