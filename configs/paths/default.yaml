# path to root directory
# this requires PROJECT_ROOT environment variable to exist
# you can replace it with "." if you want the root to be the current working directory
root_dir: ${oc.env:PROJECT_ROOT}

# path to data directory
base_data_dir: ${paths.root_dir}/data

# Optional output location, independent of the input data root.
output_root: ${oc.env:ADL1T_OUTPUT_ROOT,${paths.root_dir}}

# path to logging directory
log_dir: ${paths.output_root}/logs/

# path to output directory, created dynamically by hydra
# path generation pattern is specified in `configs/hydra/default.yaml`
# use it to store all files generated during the run, like ckpts and metrics
output_dir: ${hydra:runtime.output_dir}

# path to checkpoints directory
checkpoints_dir: ${paths.output_root}/checkpoints/

# path to working directory
work_dir: ${hydra:runtime.cwd}

# path to the raw data
raw_data_dir: ???
